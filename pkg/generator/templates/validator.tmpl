// pkg/generator/templates/validator.tmpl

// Code generated by elval-gen. DO NOT EDIT.
// Source: {{ .SourceFile }}

package {{ .Package }}

import (
"fmt"
{{- if hasTime .Structs }}
    "time"
{{- end }}
"github.com/arkannsk/elval/pkg/validator"
)

{{ range $struct := .Structs }}
    // Validate проверяет структуру {{ $struct.Name }}
    func (v *{{ $struct.Name }}) Validate() error {
    var errs []error

    {{- range $field := $struct.Fields }}
        // Валидация поля {{ $field.Name }}
        {{- if $field.Type.IsSlice }}
            {
            builder := validator.NewSliceValidator[{{ $field.Type.Name }}]("{{ $field.Name }}")
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}
                    builder.Required()
                {{- else if eq $directive.Type "min" }}
                    builder.Min({{ index $directive.Params 0 }})
                {{- else if eq $directive.Type "max" }}
                    builder.Max({{ index $directive.Params 0 }})
                {{- else if eq $directive.Type "len" }}
                    builder.Len({{ index $directive.Params 0 }})
                {{- else if eq $directive.Type "not-zero" }}
                    builder.NotZero()
                {{- end }}
            {{- end }}
            if err := builder.Validate(v.{{ $field.Name }}); err != nil {
            errs = append(errs, err)
            }
            }
        {{- else if eq $field.Type.Name "string" }}
            {
            {{- $hasRequired := false }}
            {{- $hasOptional := false }}
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}{{ $hasRequired = true }}{{ end }}
                {{- if eq $directive.Type "optional" }}{{ $hasOptional = true }}{{ end }}
            {{- end }}

            builder := validator.New[string]("{{ $field.Name }}")
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}
                    builder.AddRule(validator.Required[string]())
                {{- else if eq $directive.Type "min" }}
                    builder.AddRule(validator.MinLen({{ index $directive.Params 0 }}))
                {{- else if eq $directive.Type "max" }}
                    builder.AddRule(validator.MaxLen({{ index $directive.Params 0 }}))
                {{- else if eq $directive.Type "not-zero" }}
                    builder.AddRule(validator.NotEmpty())
                {{- else if eq $directive.Type "pattern" }}
                    {{- $pattern := index $directive.Params 0 }}
                    {{- if eq $pattern "email" }}
                        builder.AddRule(validator.Email())
                    {{- else if eq $pattern "phone" }}
                        builder.AddRule(validator.Phone())
                    {{- else if eq $pattern "uuid" }}
                        builder.AddRule(validator.UUID())
                    {{- else }}
                        builder.AddRule(validator.MatchRegexp({{ $pattern | printf "%q" }}))
                    {{- end }}
                {{- end }}
            {{- end }}

            {{- if and (not $hasRequired) $hasOptional }}
                if v.{{ $field.Name }} != "" {
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
                }
            {{- else }}
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
            {{- end }}
            }
        {{- else if eq $field.Type.Name "time.Time" }}
            {
            {{- $hasRequired := false }}
            {{- $hasOptional := false }}
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}{{ $hasRequired = true }}{{ end }}
                {{- if eq $directive.Type "optional" }}{{ $hasOptional = true }}{{ end }}
            {{- end }}

            builder := validator.New[time.Time]("{{ $field.Name }}")
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}
                    builder.AddRule(validator.Required[time.Time]())
                {{- else if eq $directive.Type "not-zero" }}
                    builder.AddRule(validator.TimeNotZero())
                {{- else if eq $directive.Type "after" }}
                    builder.AddRule(validator.After("2006-01-02", {{ index $directive.Params 0 | printf "%q" }}))
                {{- else if eq $directive.Type "before" }}
                    builder.AddRule(validator.Before("2006-01-02", {{ index $directive.Params 0 | printf "%q" }}))
                {{- end }}
            {{- end }}

            {{- if and (not $hasRequired) $hasOptional }}
                if !v.{{ $field.Name }}.IsZero() {
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
                }
            {{- else }}
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
            {{- end }}
            }
        {{- else if eq $field.Type.Name "time.Duration" }}
            {
            {{- $hasRequired := false }}
            {{- $hasOptional := false }}
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}{{ $hasRequired = true }}{{ end }}
                {{- if eq $directive.Type "optional" }}{{ $hasOptional = true }}{{ end }}
            {{- end }}

            builder := validator.New[time.Duration]("{{ $field.Name }}")
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}
                    builder.AddRule(validator.Required[time.Duration]())
                {{- else if eq $directive.Type "not-zero" }}
                    builder.AddRule(validator.DurationNotZero())
                {{- else if eq $directive.Type "min" }}
                    builder.AddRule(validator.DurationMin({{ index $directive.Params 0 }}))
                {{- else if eq $directive.Type "max" }}
                    builder.AddRule(validator.DurationMax({{ index $directive.Params 0 }}))
                {{- end }}
            {{- end }}

            {{- if and (not $hasRequired) $hasOptional }}
                if v.{{ $field.Name }} != 0 {
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
                }
            {{- else }}
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
            {{- end }}
            }
        {{- else }}
            {
            {{- $hasRequired := false }}
            {{- $hasOptional := false }}
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}{{ $hasRequired = true }}{{ end }}
                {{- if eq $directive.Type "optional" }}{{ $hasOptional = true }}{{ end }}
            {{- end }}

            builder := validator.New[{{ $field.Type.Name }}]("{{ $field.Name }}")
            {{- range $directive := $field.Directives }}
                {{- if eq $directive.Type "required" }}
                    builder.AddRule(validator.Required[{{ $field.Type.Name }}]())
                {{- else if eq $directive.Type "min" }}
                    builder.AddRule(validator.Min[{{ $field.Type.Name }}]({{ index $directive.Params 0 }}))
                {{- else if eq $directive.Type "max" }}
                    builder.AddRule(validator.Max[{{ $field.Type.Name }}]({{ index $directive.Params 0 }}))
                {{- else if eq $directive.Type "not-zero" }}
                    builder.AddRule(validator.NotZero[{{ $field.Type.Name }}]())
                {{- end }}
            {{- end }}

            {{- if and (not $hasRequired) $hasOptional }}
                var zero {{ $field.Type.Name }}
                if v.{{ $field.Name }} != zero {
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
                }
            {{- else }}
                if err := builder.Validate(v.{{ $field.Name }}); err != nil {
                errs = append(errs, err)
                }
            {{- end }}
            }
        {{- end }}
    {{- end }}

    if len(errs) > 0 {
    return fmt.Errorf("ошибки валидации: %v", errs)
    }
    return nil
    }
{{ end }}